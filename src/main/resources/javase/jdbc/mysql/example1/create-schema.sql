CREATE TABLE IF NOT EXISTS javase_jdbc_mysql_example1.student (
    `id` int(11) NOT NULL AUTO_INCREMENT,
    `firstname` varchar(10) NOT NULL,
    `create_date` datetime NOT NULL,
    PRIMARY KEY (`id`)
);